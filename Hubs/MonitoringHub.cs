using Microsoft.AspNetCore.SignalR;

namespace dotnet_services_viewer.Hubs;

public class MonitoringHub : Hub
{
    // El servidor llamará a métodos en los clientes
    // Por ahora, solo necesitamos el canal abierto
}
